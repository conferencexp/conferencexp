<configuration>
    <appSettings>
        <!-- Add any of these as necessary to setup your archiver to work properly. -->
        <!--NAMEDINSTANCE-->
        <!-- <add key="MSR.LST.ConferenceXP.ArchiveService.SQLConnectionString" value="data source=.;initial catalog=ArchiveService;integrated security=SSPI"/> -->
        <!-- <add key="MSR.LST.ConferenceXP.ArchiveService.TCPListeningPort" value="8082"/> -->
        <!-- <add key="MSR.LST.ConferenceXP.ArchiveService.PersistenceName" value="Custom Archive Service Name"/> -->
        <!-- <add key="MSR.LST.ConferenceXP.ArchiveService.PersistenceCName" value="ArchiveService@your.domain.org"/> -->
        <!-- <add key="MSR.LST.ConferenceXP.ArchiveService.DiagnosticService" value="confxp.pnw-gigapop.net"/> -->

      <!-- Use UICulture to override the service language.
           Supported UICulture values: en, zh-CN.  The service will use the Windows UICulture if 
           UICulture is not specified here.  If the selected UICulture is not supported by the service, it will
           fall back to the English default.  -->
        <!-- <add key="MSR.LST.ConferenceXP.ArchiveService.UICulture" value="zh-CN"/> -->
      
    </appSettings>

    <!-- This allows all users to see the full exception and call stack on the client.  With this, there is some small, but innate security risk.
           However, it makes handling error much simpler & more programmable on the client-side. -->
    <system.runtime.remoting>
        <customErrors mode="off"/>
    </system.runtime.remoting>

</configuration>
